// example of helper function, return empty array or rows from the database
function emptyOrRows(rows) {
    if (!rows) {
      return [];
    }
    return rows;
 }
  
module.exports = {
emptyOrRows
}